<% function slugify(text) { %>
  <% text = text.toString().toLowerCase() %>
  <% text = text.replace(/\s+/g, '-') %>           <!-- Replace spaces with - -->
  <% text = text.replace(/[^\w\-]+/g, '') %>       <!-- Remove all non-word chars -->
  <% text = text.replace(/\-\-+/g, '-') %>         <!-- Replace multiple - with single - -->
  <% text = text.replace(/^-+/, '') %>             <!-- Trim - from start of text -->
  <% text = text.replace(/-+$/, '') %>             <!-- Trim - from end of text -->
  <% return text; %>
<% } %>

<div class="container">
    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Name</th>
          <th scope="col">Email</th>
          <th scope="col">Contact</th>
          <th scope="col">Resume</th>
        </tr>
      </thead>
      <tbody>
        <% applicants.forEach(applicant=> { %>
            
            
            <tr id="<%= slugify(applicant.name) %>">

                    
                    <th scope="row">
                        <%= applicant.applicantid %>
                    </th>
                    <td>
                        <%= applicant.name %>
                    </td>
                    <td>
                        <%= applicant.email %>
                    </td>
                    <td><%= applicant.contact %>
                    </td>
                    <td>
                        <!-- <img src="<%= applicant.resumePath %>" alt="<%= applicant.name %>" style="max-width: 100px;"> -->
                        <a class="btn btn-sm btn-primary" href="<%= applicant.resumePath %>" target="_blank">view</a>
                    </td>
                </tr>
            <% }) %>
    </tbody>
    </table>
  </div>